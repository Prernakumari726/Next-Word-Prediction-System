# Next-Word-Prediction-System
Developed a next word prediction system trained on 20K+ text descriptions. Built a preprocessing pipeline using tokenization, n-gram sequence generation, and padding, then trained an LSTM model with word embeddings to predict the most probable next word. Saved the trained model and tokenizer for deployment in an interactive application and testing.
